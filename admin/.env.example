# ============================================
# AI HYBRID ADMIN DASHBOARD - ENVIRONMENT CONFIGURATION
# ============================================
# Copy this file to .env.local and fill in your actual values

# --------------------------------------------
# GATEWAY API CONFIGURATION
# --------------------------------------------
# URL of the gateway API (used for display purposes in the UI)
# This is a public variable (NEXT_PUBLIC_*) so it's accessible in the browser
NEXT_PUBLIC_GATEWAY_URL=http://localhost:8080

# --------------------------------------------
# ADMIN AUTHENTICATION
# --------------------------------------------
# Admin token for server-side API calls to the gateway
# This should match the ADMIN_TOKEN in the gateway .env file
# IMPORTANT: This is ONLY used server-side and never exposed to the browser
# TODO: Use the same admin token as in gateway/.env
ADMIN_TOKEN=supersecret_admin_token_change_this

# --------------------------------------------
# SESSION CONFIGURATION
# --------------------------------------------
# Secret key for signing session cookies
# Generate a secure random string (e.g., using: openssl rand -base64 32)
# TODO: Generate a secure random session secret
SESSION_SECRET=your_secure_session_secret_here_change_this

# Password for admin login (can be same as ADMIN_TOKEN or different)
# TODO: Set your admin password
ADMIN_PASSWORD=supersecret_admin_token_change_this
